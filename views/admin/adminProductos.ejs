<%- include("../partials/head") %>

    <body>
        <%- include("../partials/header2") %>

            <!-- *****************************INICIO MAIN****************************************** -->

            <main class="main-admin">
                <h3 class="all-product-title">Botas</h3>
                <div class="productos-admin">

                    <% products.forEach(product=> { %>


                        <% if (product.categorias.name==="Botas" && product.status === "Enabled") { %>

                            <%- include("../components/adminProductEnable",{product:product}) %>
                            
                            <% } %>
                                <% }) %>

                </div>
                <h3 class="all-product-title">Bucaneras</h3>
                <div class="productos-admin">

                    <% products.forEach(product=> { %>
                        <% if (product.categorias.name==="Bucaneras" && product.status === "Enabled") { %>

                            <%- include("../components/adminProductEnable",{product:product}) %>

                            <% } %>
                                <% }) %>

                </div>
                <h3 class="all-product-title">Borcegos</h3>
                <div class="productos-admin">

                    <% products.forEach(product=> { %>


                        <% if (product.categorias.name==="Borcegos" && product.status === "Enabled" ) { %>

                            < <%- include("../components/adminProductEnable",{product:product}) %>
                            <% } %>
                                <% }) %>

                </div>
                <h3 class="all-product-title">Guillerminas</h3>
                <div class="productos-admin">

                    <% products.forEach(product=> { %>


                        <% if (product.categorias.name==="Guillerminas" && product.status === "Enabled") { %>

                            < <%- include("../components/adminProductEnable",{product:product}) %>
                            <% } %>
                                <% }) %>

                </div>
                <h3 class="all-product-title">Texanas</h3>
                <div class="productos-admin">

                    <% products.forEach(product=> { %>


                        <% if (product.categorias.name==="Texanas"&& product.status === "Enabled" ) { %>

                            <%- include("../components/adminProductEnable",{product:product}) %>
                            <% } %>
                                <% }) %>

                </div>
                <h3 class="all-product-title">Gift-Card</h3>
                <div class="productos-admin">

                    <% products.forEach(product=> { %>


                        <% if (product.categorias.name==="Gift-Card" && product.status === "Enabled") { %>

                            <%- include("../components/adminProductEnable",{product:product}) %>
                            <% } %>
                                <% }) %>

                </div>
                <div class="news-letter">
                    <h2>Suscribite a nuestro newsletter</h2>
                    <form>
                        <input type="email" placeholder="Ingresa tu e-mail">
                        <button><i class="fa-solid fa-arrow-right"></i></button>
                    </form>

                </div>

                <div class="help-button">
                    <button><i class="fa-solid fa-circle-question"></i> Ayuda</button>
                </div>

            </main>

            <!-- ********************************FIN MAIN***************************************** -->
            <%- include("../partials/footer") %>
    </body>

    </html>